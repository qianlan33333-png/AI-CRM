schema_version: 1
owner: shared_security
parent_issue: 72

product_boundary:
  user_visible_capability_delta: none
  new_product_routes_pages_menus: none
  new_business_models_tables: none

secret_defaults: &secret_defaults
  current_sources:
    - environment
    - app_settings
  target_provider: versioned_file_secret_store
  rotation_owner: platform_ops
  rollback: previous_secret_version

secret_keys:
  SECRET_KEY:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.shared.runtime
      - aicrm_next.admin_auth.service
      - aicrm_next.shared.signed_session
      - aicrm_next.extensions.forms.questionnaire.oauth
      - aicrm_next.extensions.radar.radar_links
  AICRM_NEXT_ACTION_TOKEN_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.admin_auth.action_token
      - aicrm_next.admin_jobs.routes
  AICRM_AUTH_SESSION_HASH_PEPPER:
    <<: *secret_defaults
    purpose: server_session_hashing
    runtime_consumers:
      - aicrm_next.platform_foundation.auth_platform.sessions
      - scripts.ops.bootstrap_auth_clients
      - scripts.ops.check_auth_readiness
  AICRM_AUTH_JWT_SIGNING_KEY:
    <<: *secret_defaults
    purpose: api_access_jwt_signing
    runtime_consumers:
      - aicrm_next.platform_foundation.auth_platform.service
      - scripts.ops.bootstrap_auth_clients
      - scripts.ops.check_auth_readiness
  AICRM_QUESTIONNAIRE_OAUTH_STATE_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.extensions.forms.questionnaire.oauth
  AICRM_AI_AUDIENCE_AGENT_API_KEY:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.extensions.ai.ai_audience_ops.agent_gateway
  AICRM_RUNTIME_V2_AGENT_API_KEY:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.extensions.ai.ai_audience_ops.agent_gateway
  AICRM_HUANGYOUCAN_DB_PASSWORD:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.huangyoucan_usage_client
  AICRM_MEDIA_UPLOAD_PROVIDER_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.media_live_adapter
  AICRM_OAUTH_IDENTITY_APP_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.oauth_identity_live_adapter
  AICRM_PAYMENT_COMMERCE_PROVIDER_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.payment_commerce_live_adapter
  AICRM_WECOM_CONTACT_CALLBACK_AES_KEY:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wecom_contact_callback_live_adapter
  AICRM_WECOM_CONTACT_CALLBACK_TOKEN:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wecom_contact_callback_live_adapter
  AICRM_WECOM_GROUP_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wecom_customer_group_client
      - aicrm_next.channels.integration_gateway.wecom_media_upload_client
  AICRM_WECOM_MEDIA_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wecom_media_upload_client
  AICRM_WECOM_OPERATION_MEMBERS_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wecom_operation_members_client
  AICRM_WECOM_TAG_AGENT_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wecom_tag_live_gateway
  DEEPSEEK_API_KEY:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.extensions.ai.ai_audience_ops.agent_gateway
  AICRM_SIDEBAR_JSSDK_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wecom_jssdk_adapter
  SIDEBAR_THIRD_PARTY_API_TOKEN:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.admin_config
  WECOM_CONTACT_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wecom_tag_live_gateway
      - aicrm_next.channels.integration_gateway.wecom_operation_members_client
      - aicrm_next.channels.integration_gateway.wecom_channel_entry_client
      - aicrm_next.platform_foundation.external_effects.adapters
  WECOM_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.auth_wecom.service
      - aicrm_next.crm.identity_contact.sidebar_jssdk
      - aicrm_next.channels.integration_gateway.wecom_jssdk_adapter
      - aicrm_next.channels.integration_gateway.wecom_channel_entry_client
  WECOM_ARCHIVE_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.extensions.archive.message_archive.sync_service
  WECOM_CALLBACK_TOKEN:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.channel_entry.application
      - aicrm_next.admin_read_model.projections
  WECOM_CALLBACK_AES_KEY:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.channel_entry.application
      - aicrm_next.channels.channel_entry.wecom_crypto
  WECHAT_MP_APP_SECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.questionnaire_adapters
      - aicrm_next.extensions.forms.questionnaire.application
      - aicrm_next.extensions.commerce.public_product.h5_wechat_pay
  WECHAT_PAY_API_V3_KEY:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wechat_pay_client
      - aicrm_next.extensions.commerce.public_product.h5_wechat_pay
  WECHAT_PAY_CERT_SERIAL_NO:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wechat_pay_client
      - aicrm_next.extensions.commerce.public_product.h5_wechat_pay
  WECHAT_SHOP_APPSECRET:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.channels.integration_gateway.wechat_shop_client
      - aicrm_next.extensions.commerce.commerce.wechat_shop_service
  WECHAT_SHOP_CALLBACK_TOKEN:
    <<: *secret_defaults
    runtime_consumers:
      - aicrm_next.shared.runtime
      - aicrm_next.extensions.commerce.commerce.wechat_shop_signature
      - aicrm_next.extensions.commerce.commerce.wechat_shop_service

api_client_credential_policy:
  exact_purpose_required: true
  cross_purpose_replay: deny
  route_policy_field: client_purpose
  token_endpoint: /oauth/token
  access_token_type: signed_jwt
  default_ttl_seconds: 1800
  maximum_ttl_seconds: 3600
  refresh_tokens: disabled
  client_secret_storage: strong_hash_only
  source_secret_storage: versioned_file_secret_store
  legacy_shared_bearer_fallback: disabled

pii_audit_defaults:
  customer:
    purpose: customer_record_access
    audit_mode: required
    scope_source: route_policy
    raw_payload_allowed: false
  sensitive:
    purpose: sensitive_record_access
    audit_mode: required
    scope_source: resource_fingerprint
    raw_payload_allowed: false
  financial:
    purpose: financial_record_access
    audit_mode: required
    scope_source: resource_fingerprint
    raw_payload_allowed: false

pii_route_overrides:
  - path_prefix: /api/identity
    purpose: identity_resolution
    audit_mode: required_fail_closed
    scope_source: resource_fingerprint
    raw_payload_allowed: false
  - path_prefix: /api/messages
    purpose: message_content_access
    audit_mode: required_fail_closed
    scope_source: resource_fingerprint
    raw_payload_allowed: false
  - path_prefix: /api/archive
    purpose: message_archive_access
    audit_mode: required_fail_closed
    scope_source: resource_fingerprint
    raw_payload_allowed: false
  - path_prefix: /api/admin/questionnaires
    purpose: questionnaire_response_access
    audit_mode: required_fail_closed
    scope_source: resource_fingerprint
    raw_payload_allowed: false
  - path_prefix: /api/h5/questionnaires
    purpose: questionnaire_self_service_access
    audit_mode: required
    scope_source: resource_fingerprint
    raw_payload_allowed: false
  - path_prefix: /api/admin/radar-links
    purpose: radar_event_access
    audit_mode: required_fail_closed
    scope_source: resource_fingerprint
    raw_payload_allowed: false
  - route_names:
      - class_user_management_export
      - export_questionnaire
      - export_radar_link_events
      - export_wechat_admin_orders
      - create_admin_export
      - get_admin_export
      - user_ops_export_preview
      - user_ops_export_stub
    purpose: pii_export
    audit_mode: required_fail_closed
    scope_source: resource_fingerprint
    raw_payload_allowed: false
  - route_names:
      - api_admin_jobs_order_identity_repair_run
      - repair_entry
    purpose: identity_forced_repair
    audit_mode: required_fail_closed
    scope_source: resource_fingerprint
    raw_payload_allowed: false

outbound_webhook_callers:
  - source: aicrm_next.platform_foundation.external_effects.adapters.WebhookAdapter
    dispatch_owner: aicrm_next.external_push.https_transport
    redirect_policy: deny
    dns_policy: resolve_validate_pin
    real_execution_change: none
  - source: aicrm_next.external_push.service
    dispatch_owner: aicrm_next.external_push.https_transport
    redirect_policy: deny
    dns_policy: resolve_validate_pin
    real_execution_change: none
  - source: aicrm_next.extensions.commerce.commerce.external_push_admin
    dispatch_owner: aicrm_next.external_push.https_transport
    redirect_policy: deny
    dns_policy: resolve_validate_pin
    real_execution_change: none
  - source: aicrm_next.extensions.forms.questionnaire.external_push
    dispatch_owner: aicrm_next.external_push.https_transport
    redirect_policy: deny
    dns_policy: resolve_validate_pin
    real_execution_change: none
