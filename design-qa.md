# CRM 开放 API Key 状态确认 Design QA

## 验收范围

- 参考图：用户提供的创建确认弹窗、一次性完整 Key 弹窗、脱敏 Key 列表三张截图。
- 实现页：`/admin/config/api-key` 的空状态、生成确认、一次性凭据、关闭后当前凭据、重新生成和停用状态。
- 产品边界：系统始终只有一个 CRM 开放 API Key，不新增多 Key 列表，也不允许恢复查看完整值。

## 同屏比较

- 将三张参考图分别与相同业务状态的实际页面截图放入同一比较画布检查。
- 生成和重新生成前均使用居中确认弹窗，明确说明旧 Key 失效和完整值仅展示一次。
- 创建完成后使用独立的一次性凭据弹窗，主操作为“复制 Key”，完整值从 `aics_` 首位开始显示。
- 参考图的多 Key 列表被收敛为“正在工作的 Key”单条状态卡，保留脱敏首尾、状态、版本、时间和高风险操作。

## 功能与安全状态

- 实际接口生成和连续轮换均成功，生成后页面立即显示“正在使用”。
- 关闭一次性凭据弹窗会清空完整 Key 的 DOM 值；刷新后无法恢复完整值。
- 刷新后仍显示同一个脱敏首尾标识、当前版本和轮换时间，可直接确认哪个 Key 正在生效。
- 完整 Key 未写入 localStorage 或 sessionStorage；列表、普通状态响应和页面日志均不返回完整值。
- 历史 Key 若尚无脱敏首尾元数据，页面明确提示下次重新生成后补齐，不对哈希做不可行的反推。

## 响应式与可用性

- 桌面宽度下状态卡信息和操作保持单行主次关系，调用方式独立成卡。
- 窄屏实际视口下凭据信息改为单列、操作按钮可换行，页面无横向溢出。
- Escape、关闭按钮和取消按钮均可退出弹窗；复制按钮获得生成完成后的键盘焦点。
- 页面沿用后台唯一 PageHeader、现有 Admin Button、卡片、状态徽标和 AdminApi，不产生重复标题或第二套交互体系。

## 修正记录

- 将原先分散的状态数字和一次性输入区重构为“说明—当前 Key—调用方式”三级卡片结构。
- 增加生成与重新生成确认弹窗，避免操作后不知道是否已保存。
- 增加数据库持久化的脱敏首尾标识，关闭和刷新后仍能核对生效凭据。
- 修复复制后的空引用报错，并避免完整 Key 输入框默认滚动到末尾。
- 更新静态资源版本，避免浏览器继续加载旧脚本。

final result: passed

---

# AI-CRM 整体视觉重做 Design QA

## 最终结果

final result: passed

## 参考视觉

来源：`/Users/qianlan/Downloads/Avatar Delivery Platform (1).zip`

已按压缩包后台页面作为唯一视觉参考完成同视口验收；本地对照截图不纳入生产发布包。

## 浏览器实现

- 预览路由：`http://localhost:5017/admin/customers`
- 视口：`1440 × 856`
- 本地浏览器验收截图不纳入生产发布包。

## 同屏比较

已在相同视口下完成参考图与实现图的并排比较，并据此修正客户筛选按钮换行问题。

## 本次修正记录

1. 第一次对比发现客户筛选按钮换到第二行，将既有 wide-filter 调整为设计稿的四字段加操作区同排结构。
2. 第二次对比确认 208px 导航、52px 顶栏、Lark 蓝色 token、8px 卡片、6px 输入/按钮、紧凑表格与灰色画布均与参考稿一致。
3. 实现图使用现有 fixture 客户数据，参考图使用占位行；数据状态不同，但组件几何与客户列表现有行为不变。
4. 运行态验证 Agent 编辑器显示四个步骤，并可切换到固定素材面板；没有增加路由或 API。

## 范围保护

- 保留现有路由、字段、选择器、API，以及保存、发布和素材选择动作。
- QA 未执行外部调用、支付请求、订单创建、生产写入或 legacy fallback。
- 新增内容仅为视觉层样式和来源于设计包/仓库现有资产的展示图标。

---

# 后台侧边栏、素材库、优惠券与问卷编辑器 Design QA

## 最终结果

final result: passed

## 参考视觉与实现证据

- 侧边栏参考：用户提供的客户列表与渠道码中心截图；实现核对页：`/admin/channels`。
- 图片素材库参考：用户提供的素材库截图与“一排 8 张”复核要求；实现核对页：`/admin/image-library`。
- 优惠券参考：`codex-clipboard-0703ffd7-8123-4dbf-9272-985cc5809062.png`；实现核对页：`/admin/coupons/new`。
- 问卷参考：`codex-clipboard-4db8d781-3249-4d7f-9739-1074a568f674.png`；实现核对页：`/admin/questionnaires/1`。
- 本地实现截图与参考图已放入同一画布并排检查；截图仅用于本地 QA，不纳入发布包。

## 同屏比较结论

1. 后台路由命中的五份 `admin_console/base.html` 已完全一致；渠道页实际测得侧边栏宽度 208px、21 个导航图标、白色背景和蓝色选中态，与客户列表保持一致。
2. 图片素材库桌面视口实际计算为 8 个等宽列，每列 200.25px；卡片间距、圆角、标签密度均按紧凑素材库处理。
3. 优惠券二级页已按参考 HTML 重组为“独立标题栏、左侧四张表单卡、右侧 320px 用户端预览”；名称与金额输入会实时更新预览，原保存、商品选择、冻结与有效期逻辑不变。
4. 普通问卷编辑器实际计算为 `270px / minmax / 340px` 三栏，手机框宽 372px、圆角 24px；题目工具、手机卡片、右侧配置卡和危险操作区均与参考模板一致。
5. 优惠券和问卷均在 1024px 与 720px 视口复核，文档宽度等于视口宽度，没有横向溢出；三栏按既有响应式规则安全降级。

## 状态与交互

- 优惠券名称填写为“新客立减 100”、金额填写为 100 后，预览同步显示对应名称与 `¥100`。
- 问卷标题修改后，手机预览标题立即同步，顶部“未保存修改”状态正常出现。
- 页面运行日志无浏览器错误；本地 QA 未提交表单、未写生产数据、未触发外部调用。

---

# 渠道码入渠标签宽版弹窗 Design QA

## 参考视觉与目标状态

- 视觉来源：`/var/folders/dq/56xlfzsx05zc7vqhbl7lgv6c0000gn/T/codex-clipboard-5073f9ed-3372-4007-a3a9-a303669eaed4.png`
- 来源像素：`1672 × 1078`。
- 实现路由：`http://localhost:5019/admin/channels/1/edit`。
- 目标状态：打开“选择入渠标签”弹窗，桌面宽度下检查宽版弹窗、270px 标签组栏和两行组名。

## 实现检查

- 宽版布局只由渠道码页面通过 `layout: "wide"` 启用，问卷和其他共享选择器保持默认尺寸。
- 宽版弹窗目标尺寸为 `920 × 600`，左栏为 `270px`；标签组组名允许自然换行并最多显示两行。
- 搜索、单选、清空、取消、确认和隐藏保存字段均复用现有实现，没有新增接口或业务行为。
- JavaScript 语法检查、静态差异检查和 31 个渠道/标签契约测试通过。

## 浏览器比较阻塞

- 内置浏览器打开本地实现路由时触发 URL policy 拒绝，无法取得浏览器渲染截图、同屏比较、响应式尺寸或 console evidence。
- 未使用其他浏览器、独立 Playwright、CDP 或更换 URL/端口绕过该限制。
- 因缺少浏览器渲染实现图，字体、间距、颜色、图像与文案五个视觉面只能完成代码层检查，不能完成阻塞式 Design QA。

final result: blocked
